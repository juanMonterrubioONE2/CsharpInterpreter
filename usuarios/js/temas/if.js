window.temas = window.temas || {};

window.temas['if'] = {
    titulo: 'If',
    definicion: `
        <p>La estructura <strong>if</strong> ejecuta un bloque de código 
        solo si una condición es verdadera. Puede combinarse con 
        <strong>else if</strong> y <strong>else</strong> para manejar 
        múltiples casos.</p>
    `,
};